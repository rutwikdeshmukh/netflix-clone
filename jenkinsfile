pipeline {
    agent any
    environment {
        AWS_ACCOUNT_ID = '956630439322'
        AWS_DEFAULT_REGION = 'ap-south-1'
        API_KEY = '7975259a1710981340754da567bc7579'
        ECR_REPO_NAME = 'netflixclone-ecr'
    }

    // Retrieve an authentication token and authenticating docker client with registry
    stages  {
        stage('CLEANUP') {
                steps {
                    deleteDir()
                }
        }

        stage('AUTHENTICATING DOCKER CLIENT WITH AWS ECR') {
                steps {
                    script {
                    sh "aws ecr get-login-password --region ${AWS_DEFAULT_REGION} | docker login --username AWS --password-stdin ${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_DEFAULT_REGION}.amazonaws.com"
                    }
                }
        }

        // Cloning into source code repository
        stage('GIT CLONE') {
            steps {
                sh 'git clone https://github.com/rutwikdeshmukh/netflix-clone.git'
            }
        }

        stage('CHECK WORKING DIRECTORY AND FILES') {
            steps {
                dir('/var/lib/jenkins/workspace/CI/netflix-clone/') {
                    sh 'ls'
                    sh 'pwd'
                }
            }
        }

        // Building the docker images
        stage('BUILDING IMAGE') {
            steps {
                script {
                    dir('/var/lib/jenkins/workspace/CI/netflix-clone/') {
                        sh 'envsubst < Dockerfile > DockerfileTemp'
                        sh 'docker build -f DockerfileTemp -t netflix-clone .'
                    }
                }
            }
        }

        // Tagging the docker image
        stage('TAGGING IMAGE') {
            steps {
                script {
                    dir('/var/lib/jenkins/workspace/CI/netflix-clone/') {
                        sh 'docker tag netflix-clone:latest ${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_DEFAULT_REGION}.amazonaws.com/${ECR_REPO_NAME}:netflix-clone-latest'
                    }
                }
            }
        }

        // Pushing the Docker image to ECR
        stage('PUSH TO ECR') {
            steps {
                script {
                    dir('/var/lib/jenkins/workspace/CI/netflix-clone/') {
                        sh 'docker push ${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_DEFAULT_REGION}.amazonaws.com/${ECR_REPO_NAME}:netflix-clone-latest'
                    }
                }
            }
        }
    }
}